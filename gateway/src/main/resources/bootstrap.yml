spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
server:
  port: 8081
eureka:
  client:
    service-url:
#      defaultZone: http://discovery:8080/eureka
      defaultZone: http://jiangtianxing:jiangtianxing@localhost:8761/eureka
test: 1, 2, 3, 4, 5, 6