spring:
  application:
    name: user-center
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
server:
  port: 8080
eureka:
  client:
    service-url:
      defaultZone: http://jiangtianxing:jiangtianxing@discovery:8080/eureka