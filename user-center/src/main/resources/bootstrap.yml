spring:
  application:
    name: user-center
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
