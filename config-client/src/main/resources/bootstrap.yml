spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:9000
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
server:
  port: 8080
