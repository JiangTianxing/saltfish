spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://config-server:8080
  rabbitmq:
      host: rabbitmq
      port: 5672
      username: guest
      password: guest
server:
  port: 8080