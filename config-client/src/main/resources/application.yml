spring:
  application:
    name: config-client
  cloud:
    config:
      discovery:
        service-id: config-server
      label: master
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://discovery:8080/eureka
server:
  port: 8080